@model List<PortalCG.Models.ViewModels.LeaderViewModel>
    @{
        ViewBag.Title = "Dirigentes";
        const int ITEM_PER_ROW = 3;
    }
    @if (Model?.Count > 0)
    {
        <div class="row">
            @foreach (var item in Model)
            {
                <div class="col-md-@(12/ITEM_PER_ROW)">
                    TESTE
                    @*@Html.Partial("_DisciplinePartial", item)*@
                </div>
            }
        </div>
    }